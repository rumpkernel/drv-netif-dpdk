Example TCP HTTP GET
====================

Fetch index.html from www.netbsd.org port 80.  This is in fact the
same program as examples/webbrowser in the buildrump.sh repo, but
included here for convenience.
